{
  "buildCommand": "cd PetPal && npx expo export -p web",
  "outputDirectory": "PetPal/dist",
  "devCommand": "npx expo",
  "cleanUrls": true,
  "framework": null,
  "installCommand": "cd PetPal && npm install",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
