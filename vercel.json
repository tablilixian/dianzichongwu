{
  "buildCommand": "cd PetPal && npm run build:web",
  "outputDirectory": "PetPal/web-build",
  "devCommand": "cd PetPal && npx expo start --web",
  "cleanUrls": true,
  "framework": null,
  "installCommand": "cd PetPal && npm install",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
