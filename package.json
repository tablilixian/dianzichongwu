{
  "name": "dianzichongwu",
  "version": "1.0.0",
  "scripts": {
    "build": "cd PetPal && npm install --legacy-peer-deps && npx expo export:web"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}